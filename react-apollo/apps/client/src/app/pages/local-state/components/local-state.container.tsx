import React, { FunctionComponent } from 'react';

import { LocalStatePresentational } from './local-state.presentational';

type Props = {};

export const LocalState: FunctionComponent<Props> = (props) => {
  return <LocalStatePresentational />;
};
